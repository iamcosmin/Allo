import 'package:allo/core/main.dart';

class Core {
  static Alerts alerts = Alerts();
  static Navigation navigate = Navigation();
  static Auth auth = Auth();
  static Chats chats = Chats();
}
// Down here is some misc plugins that don't really extend core but we need for
// smooth operations.


